box:
  id: webstream/php-alpine
  cmd: /bin/sh
build:
  steps:
    - script:
        name: classloader test
        code: |
          docker run -v ${WERCKER_SOURCE_DIR}:/workspace/ webstream/php-alpine vendor/bin/phpunit --configuration phpunit.xml
